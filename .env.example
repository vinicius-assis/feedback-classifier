# Copy to `.env` and adjust values. Mongo: `docker compose up -d` (see docker-compose.yml).
MONGODB_URI=mongodb://localhost:27017/ledn_feedback
OPENAI_API_KEY=sk-...
SLACK_INGEST_SECRET=changeme
CORS_ORIGIN=http://localhost:5173
PORT=3000
